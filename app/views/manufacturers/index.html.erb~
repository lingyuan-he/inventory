<h1>List all Manufacturers</h1>
 
<table border="1">
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th>Address</th>
    <th>Contact's Name</th>
    <th>Contact's Phone</th>
    <th>Contact's Email</th>
    <th colspan = "3"> </th>
  </tr>
 
  <% @manufacturers.each do |manufacturer| %>
    <tr>
      <td><%= manufacturer.id %></td>
      <td><%= manufacturer.name %></td>
      <td><%= manufacturer.address %></td>
      <td><%= manufacturer.contact_name %></td>
      <td><%= manufacturer.phone %></td>
      <td><%= manufacturer.email %></td>
      <td><%= link_to 'Details', manufacturer_path(manufacturer) %></td>
      <td><%= link_to 'Edit', edit_manufacturer_path(manufacturer) %></td>
      <td><%= link_to 'Destroy', manufacturer_path(manufacturer), :method => :delete, :confirm: => 'Are you sure?' %></td>
    </tr>
  <% end %>
</table>
